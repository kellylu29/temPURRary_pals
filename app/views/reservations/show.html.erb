<div class="reservation_page">
  <div class="res_container">
    <p><strong>Reservation Date:</strong><br>
      <%= @reservation.date.strftime("%m / %d / %Y") %>
    </p>

    <p><strong>Cat:</strong></p>
    <div class="cat_img" style='background-image: url("<%= @reservation.pet.avatar.url %>")'>
    </div>
    <p><strong>Name:</strong><br>
      <%= @reservation.pet.name %></p>
    <p><strong>Description:</strong><br>
      <%= @reservation.pet.description %></p>
    <p><strong>Location:</strong><br>
      <%= @reservation.pet.city %>, <%= @reservation.pet.state %></p>
    <p><%= link_to 'Edit', edit_reservation_path(@reservation), :class => 'link' %> |
      <%= link_to 'Delete', @reservation, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'link' %>
    </p>
  </div>

</div>
